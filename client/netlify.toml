[build]
  clear_cache = true
  functions = "netlify/functions"
  node_bundler = "esbuild"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/sitemap_index.xml"
  to = "/sitemap.xml"
  status = 301
  force = true

[functions]
  node_bundler = "esbuild"

[dev]
  targetPort = 3001